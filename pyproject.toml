[project]
name = "caching-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.10"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "isort>=6.0.1",
]
